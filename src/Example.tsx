import React, { useEffect } from "react";
// import useDynamicRef from "./useDynamicRef";

// const Example = () => {
//   const foo = ["random_id_1", "random_id_2"];
//   const [getRef, setRef] = useDynamicRef();

//   useEffect(() => {
//     // Get ref for specific ID
//     const id1 = getRef("random_id_1");
//     console.log(id1);
//   }, []);

//   return (
//     <>
//       {/* Simple set ref. */}
//       <span ref={setRef("random_id_1")}></span>

//       {/*  Set refs dynamically in Array.map() */}
//       {foo.map((eachId, idx) => (
//         <div ref={setRef(eachId)}> Hello {each} </div>
//       ))}
//     </>
//   );
// };

// export default Example;
